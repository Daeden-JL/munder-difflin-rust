[package]
name = "md-hook"
version.workspace = true
edition.workspace = true
license.workspace = true

# No dependencies, deliberately. This runs on EVERY hook of every agent — many
# times per turn — so its cost is startup time, and a dependency tree would be
# paid over and over for no benefit. std has everything a byte relay needs.
[dependencies]
